/*
Written in the D programming language.
For git maintenance (ensure at least one congruent line with originating C header):
#define crypto_uint64_H
*/

module sodium.crypto_uint64;

alias crypto_uint64 = ulong;
